name: koality_tools
description: A collection of useful scripts for various projects.
version: 0.1.1
repository: https://dart.cloudsmith.io/code-koalas/koality/
publish_to: https://dart.cloudsmith.io/code-koalas/koality/

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  args: ^2.3.1
  cli_completion: ^0.1.0+1
  mason_logger: ^0.2.0
  pub_updater: ^0.2.1

dev_dependencies:
  build_runner: ^2.0.0
  build_verify: ^3.0.0
  build_version: ^2.0.0
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^3.1.0

executables:
  koality:
